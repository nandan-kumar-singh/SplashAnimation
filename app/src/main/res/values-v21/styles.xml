<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="MaterialTheme" parent="AppTheme">
        <item name="android:windowContentTransitions">true</item>

        <!-- specify enter and exit transitions -->
        <item name="android:windowEnterTransition">@transition/explode</item>
        <item name="android:windowExitTransition">@transition/explode</item>

        <!-- specify shared element transitions -->
        <item name="android:windowSharedElementEnterTransition">@transition/shared_resources</item>
        <item name="android:windowSharedElementExitTransition">@transition/shared_resources</item>
        ...
    </style>
</resources>